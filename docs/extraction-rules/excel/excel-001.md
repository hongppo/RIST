---
type: planning
hub: "[[💡planning]]"
created_at: 2026-09-25
updated_at: 2026-09-25
client: "[[RIST(포항산업과학연구원)]]"
project: "[[RIST 데이터 표준화 프로그램 개발]]"
status: 초안
version: 1
---

## 변경 이력

| 수정일 | 변경 항목 | 변경 내용 |
| --- | --- | --- |
| 2026/09/25 | 최초 작성 | 시료 열·물질 행의 VOCs 결과 행렬 판별, 실제 0과 빈 양식 열 구분, 원문 정밀도 보존 및 지역·분석법 매칭 정의 |

## 규칙 개요

| 항목 | 내용 |
| --- | --- |
| 규칙 코드 | `EXCEL-001` |
| 대상 | 상단에 시료명, 왼쪽에 분석물질명, 교차 셀에 결과값이 배치된 VOCs 결과 XLSX |
| 샘플 파일명 | `VOCs 결과 양식_조용선 - 복사본.xlsx` |
| 파서 추출 단위 | 시료 열별 기본 기록 1건과 해당 열의 물질별 결과 N건 |
| 추출 범위 | 유효한 시료명·분석물질명에 연결된 결과 행렬 |
| 기본 항목 매핑 | [통합 스키마 · review_record](<../../rist-schema.md#review_record>) |
| 물질별 항목 매핑 | [통합 스키마 · review_result](<../../rist-schema.md#review_result>) |
| 규칙 목록 | [추출 규칙](<../index.md#excel>) |

## 파일 읽기 및 유형 판별

| 항목 | 처리 기준 |
| --- | --- |
| 파일 읽기 | XLSX의 시트·셀 주소·셀 유형·공유 문자열·숫자 원문·표시 형식을 함께 읽음. 원본 파일은 수정하지 않음 |
| 결과표 구성 | 상단의 여러 시료명 열, 왼쪽의 여러 물질명 행, 각 교차 셀의 숫자 또는 결과 표기가 연결된 행렬 탐색 |
| 물질 축 | Benzene·Toluene 등 VOCs 물질명이 세로로 이어지는지 확인. 물질 이름 하나만으로 유형을 확정하지 않음 |
| 유형 확정 | 시료 열·물질 행의 대응과 결과 영역이 함께 확인될 때 확정. 다른 규칙 후보와 중복되거나 축 대응이 불명확하면 확인 대상으로 반환 |
| 가변 구조 | 파일명·시트명·셀 주소·시료 및 물질 수·빈 열 개수는 판별·종료의 고정 조건으로 사용하지 않음 |
| 시트·블록 | 각 시트에서 결과표 블록을 독립 탐색. 시트 순서와 블록의 원본 위치를 보존하며 다른 블록의 헤더를 임의 상속하지 않음 |
| 병합 셀 | 헤더가 병합된 경우 실제 병합 범위와 하위 열의 역할을 확인. 시료 대응이 유일하지 않으면 임의 복제하지 않고 확인 처리 |

## 추출 구간 및 빈 양식 열 구분

| 구분 | 판정 및 처리 |
| --- | --- |
| 시작 | 시료명 헤더와 분석물질명 축을 찾고 교차 결과 영역을 구성 |
| 유효 시료 열 | 비어 있지 않은 시료명과 결과 행렬의 연결이 확인된 열. 시료명 자체를 DB 식별키로 사용하지 않음 |
| 물질 행 | 물질명이 기재되고 시료 열의 결과와 대응하는 행. 제목·주석·빈 행을 물질로 포함하지 않음 |
| 빈 양식 열 | 시료명 헤더가 숫자 `0`이고 물질 행의 결과도 모두 숫자 `0`인 열은 제외 |
| 실제 0 | 유효한 시료명 아래의 `0`은 정상 결과. 해당 열의 결과가 모두 0이어도 포함 |
| 이름 없는 값 | 시료명 없이 0 이외의 값이 있거나 축 대응이 불분명하면 원문·위치를 보존하고 확인 처리. 빈 양식으로 일괄 삭제하지 않음 |
| 빈 셀·간격 | 중간의 빈 셀·빈 열만으로 결과표 종료를 단정하지 않음. 다음 유효 헤더·물질·결과의 연결을 확인 |
| 종료 | 해당 시료·물질 축에 대응하는 결과가 끝나거나 별도 표·주석 영역으로 전환되는 지점 |
| 반복 시료 | 같은 이름의 별도 열은 위치·출현 순서를 보존. 임의 병합·평균·중복 제거하지 않음 |

### 샘플 구간 대조

| 영역 | 샘플 위치 | 내용 |
| --- | --- | --- |
| 시료명 | Sheet1 · C1:CX1 | 시료 100개 |
| 분석물질 | Sheet1 · B2:B19 | 물질 18개 |
| 결과 | Sheet1 · C2:CX19 | 1,800개. 실제 0은 561개, 빈 셀은 없음 |
| 제외 영역 | Sheet1 · CY1:IH19 | 헤더와 결과가 모두 숫자 0인 140열 |

## 공통 항목 추출

### 시료 연결

| 항목 | 처리 기준 |
| --- | --- |
| 시료명 | 유효한 헤더 셀 문자열 전체를 추출. 앞뒤 공백만 정리하고 지역·시료번호·하이픈 등 내용 보존 |
| 시료별 기본 기록 | 시트→블록→왼쪽에서 오른쪽 시료 열 순서로 `record_order` 부여 |
| 결과 연결 | 같은 열과 교차하는 각 물질 행의 결과를 그 시료의 `results[]`에 연결 |
| 시료번호 | `청림 1-1`, `태인 12-5` 등의 번호를 날짜·기간·시간으로 해석하지 않음 |

### 공통 항목 추출 예시 및 컬럼 매핑

| 항목 | 매핑 컬럼 | 샘플 원본 위치·값 | 실제 추출 예시 | 추출 규칙 |
| --- | --- | --- | --- | --- |
| 시료 순서 | 파서 `record_order` | Sheet1 · C열 첫 유효 시료 | `1` | SYSTEM. 블록의 시료 열 순서 |
| 시료명 | `sample_name` | Sheet1 · C1 `청림 1-1` | `청림 1-1` | EXTRACTED. 해당 열의 모든 물질 결과에 연결 |
| 마지막 시료 | `sample_name` | Sheet1 · CX1 `태인 12-5` | `태인 12-5` | 마지막 시료 위치는 파일별로 탐색 |

### 지역·관측 지점 및 좌표 매칭

시료명이나 지역에 [지역 매칭](<../../matching-rules/location.md>)의 매칭 단어가 포함되면 해당 관측 지점에 연결하고 [지역 DB](<../../db-lists/location.md>)의 위도·경도를 사용한다. 매칭 단어가 시료명 앞부분에 있어야 한다는 조건은 두지 않는다.

| 시료명·지역에 포함된 단어 | 관측 지점명 |
| --- | --- |
| 청림 | 포항 청림 |
| 해도 | 포항 해도 |
| 묘도, 온동 | 여수 묘도(온동) |
| 태인 | 광양 태인 |

- 위 표는 샘플의 연결 예시이며 전체 매칭 단어는 지역 매칭 문서를 따른다.
- 같은 지점에 해당하는 후보는 하나로 처리. 서로 다른 지점이 겹치거나 대응이 없으면 위치값은 NULL로 두고 확인 처리한다.
- 지점명·위도·경도는 같은 DB 행에서 가져와 MATCHED로 기록한다. 첫 시료의 좌표를 모든 시료에 적용하지 않는다.
- `location_id`는 실제 목록 연결이 확인될 때만 설정. DB 원본 좌표는 보존하고 저장값은 `DECIMAL(13,10)` 기준으로 한 번만 반올림한다.

| 항목 | 매핑 컬럼 | 샘플 원본·DB 값 | 실제 매칭 예시 |
| --- | --- | --- | --- |
| 관측 지점 | `location_name` | 시료명 `청림 1-1` | `포항 청림` |
| 위도·경도 | `latitude`, `longitude` | DB `35.9973660373914`, `129.405711966399` | `35.9973660374`, `129.4057119664` |

## 물질별 결과 추출

### 행렬 변환

| 항목 | 처리 기준 |
| --- | --- |
| 분석물질 | 해당 행의 물질명 전체를 보존. `M,P-Xylene`을 쉼표로 분리하거나 `O-Xylene`과 병합하지 않음 |
| 결과 셀 | 유효 시료 열과 물질 행이 교차하는 셀 하나를 결과 1건으로 생성 |
| 결과 순서 | 각 시료 안에서 물질 행의 위→아래 순서로 `result_order` 부여 |
| 누락값 | 식별한 빈 셀의 결과는 NULL로 유지. 다음 행·열의 값을 당겨 채우지 않음 |
| 단위 | 샘플에는 단위가 없으므로 NULL. 물질명·파일명·숫자 크기에서 단위를 추정하지 않음 |

### 물질별 추출 예시 및 컬럼 매핑

| 항목 | 매핑 컬럼 | 샘플 원본 위치·값 | 실제 추출 예시 | 추출·변환 규칙 |
| --- | --- | --- | --- | --- |
| 결과 순서 | `result_order` | Sheet1 · B2 첫 물질 | `1` | SYSTEM. 물질 행 순서 |
| 분석물질 | `substance_name` | Sheet1 · B2 `Benzene` | `Benzene` | EXTRACTED. 같은 행 결과에 연결 |
| 분석물질 원문 | `substance_name` | Sheet1 · B4 `M,P-Xylene` | `M,P-Xylene` | 하나의 물질명으로 유지 |
| 결과 원문·표시값 | `result_raw`, `result_text` | Sheet1 · C2 저장 숫자 `0.54499999999999993` | `0.54499999999999993` | XLSX 내부 숫자 문자열 그대로 보존 |
| 숫자 결과 | `result_value` | 같은 셀의 저장 숫자 | 정밀 소수 `0.54499999999999993` | Decimal 변환. 임의 반올림 금지 |
| 실제 0 | `result_raw`, `result_text`, `result_value` | Sheet1 · N3 `0` | 원문·표시 `0`, 수치 `0` | 유효 시료 열의 실제 결과 |

### 원문 정밀도 및 셀 유형

| 구분 | 처리 기준 |
| --- | --- |
| 저장 숫자 | XLSX worksheet XML의 숫자 `<v>` 문자열을 원문으로 확보하고 Decimal로 해석. 셀 표시 형식·화면의 반올림값·이진 부동소수점 변환값으로 원문을 대체하지 않음 |
| 정밀도 전달 | JSON 등에서 정밀 소수를 문자열로 전달. 저장 컬럼이 원문 정밀도를 수용하지 못하면 원문을 보존하고 검증 오류로 반환하며 자동 절삭·반올림하지 않음 |
| 문자형 숫자 | 부호·소수·지수 표기를 포함한 전체 문자열이 유효한 숫자인지 검증 후 Decimal 변환. 원래 셀 유형·문자열은 보존 |
| 비수치 | `ND`, `n.d`, 부등호 등을 원문·표시값에 보존하고 `result_value`는 NULL. 0이나 검출한계로 대체하지 않음 |
| 빈 셀 | 식별한 빈 셀의 원문은 빈 문자열, 업무값은 NULL. 위치 자체를 식별하지 못하면 원문도 NULL과 확인 정보 기록 |
| 오류·수식 | 오류값은 원문 보존 후 수치 NULL. 수식은 수식과 캐시 결과를 구분해 보존하고 유효한 캐시만 해석. 캐시가 없거나 오류이면 확인 처리하며 외부 연결·매크로 실행으로 보완하지 않음 |

### 분석법 DB 매칭

분석물질이 [분석법 매칭](<../../matching-rules/analysis-method.md>)의 항목에 대응하면 파일 유형과 관계없이 해당 [분석법 DB](<../../db-lists/analysis-method.md>)를 연결한다. 매칭값은 파일 직접 추출값과 구분한다.

| 분석물질 | 시험방법 코드 | 분석법 표시값 |
| --- | --- | --- |
| Toluene | `ES 09307.b` | `ES 09307.b 휘발성유기화합물 - 저온 농축 - 기체크로마토그래피` |
| Styrene | `ES 09306.1b` | `ES 09306.1b 스타이렌 - 저온 농축 - 기체크로마토그래피` |

- 매칭표에 없는 물질은 분석법 NULL로 유지하고 검토 화면에서 보완한다. VOCs 파일이라는 이유만으로 전체 물질에 `ES 01804.2a`를 일괄 적용하지 않는다.
- `M,P-Xylene`, `O-Xylene`을 `Xylene`과 연결하는 별도 대응이 없으면 임의로 동일시하지 않는다. 명백한 오타도 대응이 유일할 때만 연결한다.
- 표시값은 제공된 고시번호·시험방법 코드·분석법명을 연결한다. 비어 있는 고시번호를 ES 코드로 채우지 않으며 분석법 ID는 실제 목록 연결이 확인될 때만 설정한다.
- 같은 시험방법 코드의 DB 행이 중복되면 분석물질 대응과 실제 항목을 확인한다. 코드만 보고 임의의 행 ID를 선택하지 않는다.

## 미추출 항목 및 사용자 보완

| 항목 | 처리 기준 |
| --- | --- |
| 구분·매체·담당자·분석기관 | NULL. 가이드의 인터페이스 입력 예시나 파일명에서 자동 생성하지 않음 |
| 채취 날짜·일시·정밀도 | NULL. 시료번호에서 날짜를 만들지 않음 |
| 분석 날짜·일시·정밀도·기간 | NULL. 날짜가 없을 때 기간을 1일로 생성하지 않음 |
| BASE·채취법 | 값과 관련 ID·코드·고시번호 NULL. 기존 검토 인터페이스에서 보완 |
| 단위 | NULL. 분석법·물질군에서 임의 추정하지 않음 |
| 위치·분석법 | 파일 직접 추출값은 없음. 공통 매칭에 성공한 항목만 MATCHED로 보완하며 나머지는 NULL |
| 목록 ID | 실제 DB 항목에 연결된 근거가 있을 때만 설정. 추출한 물질명 자체는 미매칭이어도 보존 |

## 원본 보존·오류 처리

| 항목 | 처리 기준 |
| --- | --- |
| 원본 위치 | 시트명·블록 식별·시료 헤더 주소·물질 헤더 주소·결과 셀 주소를 보존 |
| 원본 스냅샷 | 셀 유형·원문 문자열·숫자 저장 문자열·표시 형식·수식/캐시 구분과 매칭 근거를 보존 |
| 직접 추출 | 시료명·분석물질·결과는 `was_extracted=true`, `value_source=EXTRACTED` |
| 매칭·계산 | DB에서 보완한 지역·좌표·분석법은 MATCHED, 순서는 SYSTEM. 미추출 필드를 매칭해도 `was_extracted=false` 유지 |
| 최초 상태 | 최초 `is_modified=false`. 정상 값은 `validation_issues=[]`, 오류 범위는 원문과 확인 사유를 함께 반환 |
| 파일·구조 오류 | 암호화·손상·필수 축 식별 실패는 정상 완료로 처리하지 않음. 오류 영역 때문에 확인된 다른 결과를 삭제하지 않음 |

## 공통 처리 연결

| 대상 | 처리 기준 |
| --- | --- |
| 파서 반환 | 시료별 `records[].basic` + `records[].results[]` 1:N. 샘플은 기본 기록 100건, 각 결과 18건 |
| 매칭 계층 | 지역·분석법·목록 연결은 공통 계층에서 수행. 파서는 원문·위치·관계를 반환하며 DB 조회·ID 발급을 수행하지 않음 |
| 저장 변환 | 결과마다 basic을 복사한 독립 `review_record`·`review_result` 1:1 쌍 생성. 샘플은 1,800쌍 |
| 순서 | 파서 순서를 `source_record_order`에 보존하고 시료 열 순서→물질 행 순서로 펼쳐 DB `record_order` 부여 |
| 최종 저장 | 검토·검증 후 `analysis_record`·`analysis_result` 반영. 원문 정밀도와 원본 스냅샷 유지 |

## 가이드 근거

| 자료 | 반영 내용 |
| --- | --- |
| `RIST_파일 규칙 정리_20260915_01.xlsx` · `EXCEL-001` | 시료 열·물질 행·결과 교차표, 빈 양식 열 제외, 시료별 지역 연결, 미추출 항목 구분 |
| 지역·분석법 DB 및 매칭 규칙 | 시료별 지점·좌표 및 분석물질별 분석법 연결 |
| 통합 스키마 | 원본 보존·값 출처·정밀도·결과별 독립 저장 |
